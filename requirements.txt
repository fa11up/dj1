# AutoDJ Engine — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Audio Analysis ─────────────────────────────
librosa==0.10.2
aubio==0.4.9
soundfile==0.12.1
numpy==1.26.4

# ── Audio Manipulation & Effects ───────────────
pydub==0.25.1
pedalboard==0.9.9
pyrubberband==0.3.0

# ── Metadata ───────────────────────────────────
mutagen==1.47.0

# ── Spotify Integration ────────────────────────
spotipy==2.24.0
python-dotenv==1.0.1

# ── Matching & Utilities ───────────────────────
thefuzz==0.22.1        # fuzzywuzzy successor (no deprecation warnings)
python-Levenshtein==0.25.1  # speeds up thefuzz significantly
joblib==1.4.2
pyyaml==6.0.2
tqdm==4.66.4
colorama==0.4.6        # colored terminal output (cross-platform)
rich==13.7.1           # pretty tables and progress in CLI
